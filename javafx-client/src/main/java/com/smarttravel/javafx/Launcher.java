package com.smarttravel.javafx;

/**
 * Separate launcher class — required so JavaFX can run
 * from an unnamed module (no module-info.java needed).
 */
public class Launcher {
    public static void main(String[] args) {
        MainApp.main(args);
    }
}
